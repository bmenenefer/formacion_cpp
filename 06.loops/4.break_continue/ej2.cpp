/*
Escribe un programa que muestre al usuario la suma de número actual (empezando por 0). Luego
se le preguntará al usuario si quiere sumar el siguiente número (incrementando en 1 en un 
bucle infinito). Si el usuario ingresa y/Y, se sumará el número indicado. Si se introduce
n/N, el número indicado se saltará y se incrementará. Si el usuario introduce algo que no
y/Y/n/N el bucle finalizará.
*/
int main()
{
	char mssg[] = 'Hola';
	cout << touper(mssg) << endl;
}
