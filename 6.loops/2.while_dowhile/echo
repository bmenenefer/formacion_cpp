/*
Escribe un programa que solicite al usuario introducir un código pin por primera vez. Luego
usando while (o dowhile) se solicitará al usuario que introduzca de nuevo el código. Si el
código es incorrecto, el usuario deberá volverlo a introducir hasta que sea correcto.
*/

#include <iostream>
using namespace std;

int pin1, pin2;

main()
{
	cout << "Introduce un código pin: ";
	cin >> pin1;
	
	do{
		cout << "Inserta el pin.";
		cin >> pin2;
	}while(pin1 != pin2 && cout << "Pin incorrecto. Prueba otra vez" << endl);
}
